/** Client-side label constants mirrored from the host module (kept literal to stay import-free at runtime). */

/** Human label used in logs/badges: "ETeams v<version>". */
export const PLUGIN_VERSION_LABEL = 'v0.2.13';
